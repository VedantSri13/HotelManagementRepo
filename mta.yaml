_schema-version: 3.3.0
ID: HotelBookingSystem
description: A simple CAP project.
version: 1.0.0
modules:
- name: HotelBookingSystem-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: HotelBookingSystem-db
  - name: uaa_HotelBookingSystem
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: HotelBookingSystem-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: HotelBookingSystem-db
  parameters:
    buildpack: nodejs_buildpack
- name: HotelBookingSystem-destination-content
  type: com.sap.application.content
  requires:
  - name: HotelBookingSystem-destination-service
    parameters:
      content-target: true
  - name: HotelBookingSystem_html_repo_host
    parameters:
      service-key:
        name: HotelBookingSystem_html_repo_host-key
  - name: uaa_HotelBookingSystem
    parameters:
      service-key:
        name: uaa_HotelBookingSystem-key
  parameters:
    content:
      instance:
        destinations:
        - Name: HotelMangement_HotelBookingSystem_html_repo_host
          ServiceInstanceName: HotelBookingSystem-html5-app-host-service
          ServiceKeyName: HotelBookingSystem_html_repo_host-key
          sap.cloud.service: HotelMangement
        - Authentication: OAuth2UserTokenExchange
          Name: HotelMangement_uaa_HotelBookingSystem
          ServiceInstanceName: HotelBookingSystem-xsuaa-service
          ServiceKeyName: uaa_HotelBookingSystem-key
          sap.cloud.service: HotelMangement
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: HotelBookingSystem-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: HotelBookingSystem_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - hotelbooking.zip
      name: hotelbooking
      target-path: resources/
- name: hotelbooking
  type: html5
  path: app/hotelbooking
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: HotelBookingSystem-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: HotelBookingSystem-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: HotelBookingSystem-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: HotelBookingSystem_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: HotelBookingSystem-html5-app-host-service
    service-plan: app-host
- name: uaa_HotelBookingSystem
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: HotelBookingSystem-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
